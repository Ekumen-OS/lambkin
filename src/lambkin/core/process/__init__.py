"""Process execution utilities for benchmark runs.

Provides two execution modes:
- background: non-blocking process launch via subprocess.Popen
- foreground: blocking process execution via subprocess.run
"""
