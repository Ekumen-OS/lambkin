"""Data ingestion layer for lambkin benchmarks.

Handles all data sourcing concerns, providing a consistent
interface to pull datasets and artifacts into a benchmark run
from external storage backends.
"""
